{
  "name": "animacraft",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "The Fully onchain Character Maker & Creator",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test",
    "check": "node --check app.js && node --check chain-runtime.js && node --check draft-store.js && node --check manifest-validation.js && node --check recipe-hash.js && node --check remote-read.js && npm test && npm run build",
    "move:build": "cd move/animacraft && sui move build",
    "move:test": "cd move/animacraft && sui move test"
  },
  "dependencies": {
    "@mysten/dapp-kit-core": "1.6.4",
    "@mysten/sui": "2.20.2",
    "@mysten/walrus": "1.2.4",
    "@mysten/walrus-wasm": "0.3.0"
  },
  "devDependencies": {
    "vite": "8.1.4"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
