# Current phase

- Phase: `v8-mainnet-release-and-vercel-cutover`
- Status: `in_progress`
- Branch: `codex/v8-mainnet-release`
- Objective: finish the audited release runner, publish and attest all seven v8 packages plus init/bootstrap on Sui Mainnet, replace all production JSON-RPC transport with gRPC plus GraphQL discovery, generate real production configuration, and deploy the verified build to Vercel production.
- Constraints: no guessed secrets or commitments; no replacement signing for uncertain outcomes; irreversible Mainnet or production actions only after all documented hard gates pass; protected decryption remains explicitly unavailable until a server-only Enoki credential path is supplied and tested.
- Acceptance: focused and full Web/Move/Protocol133 gates pass; release WAL reaches verified COMPLETE with exact chain certificate; production source/bundle contains no Sui JSON-RPC client or fallback and uses gRPC for authoritative reads/writes plus GraphQL for indexed discovery; production config contains no placeholders; Vercel production and custom domain serve the attested commit/config; rollback evidence is recorded.
- Detailed spec: `docs/codex/MAINNET_V8_RELEASE_SPEC.md`
- Verified so far: production transport is gRPC authority plus GraphQL discovery with no JSON-RPC fallback; Web 291/291, seven-package Move 266/266, Protocol 133 reproducibility, adversarial probes, field/size gates, Vite build, and fresh source/dist scans pass after normalizing `std::type_name` into canonical `0x...::module::Type` notation at the protocol payment-coin boundary. Core is 63,961 bytes with 39 bytes of the repository target headroom; `MakerRootV8` remains 31 fields and all 244 production structs are at or below Protocol 133's 32-field verifier limit.
- Abandoned release boundary: execution plan `f449b142c13fad6a4cdbf260f4f49bc21fa2c68e6c2265cd270f01ba4d227351`, release `6bbca6ae971c14664f9c3c811f8e80d19665818c566dcb83b613662049f15db6`, and state directory `/Users/naoer/Documents/Claude/Projects/soulidity/releases/mainnet-v8-e8fd0ad-20260823` reached a sealed seven-package manifest but failed the ordinal 7 dry-run before signing with `EPaymentCoinMismatch` in `protocol_config_v8::initialize_protocol_treasury_v8`. The tuple is permanently inert: its Core ProtocolConfig `0x48102b59e010c341f33995f9a441091dcc3727c2459eca46c1399fef7a21504c` remains revision 0, treasury `null`, enabled `false`; no init/bootstrap transaction was signed or broadcast and no catalog/config was created. The seven immutable package objects and UpgradeCaps remain auditable orphan artifacts and must never be mixed into a new release.
- Current boundary: the payment-coin canonicalization fix and exact regression have passed every local release gate. No new Mainnet signature or broadcast has occurred for the replacement release. `protectedDecryptionReady` remains false because the Enoki server-only credential path is not an input to this release.
- Next action: freeze and push the fix commit, create a fresh state directory and execution plan, publish and certify a new Core, then perform an unsigned Mainnet init simulation against that exact new Core before spending gas on ordinals 1–6. If it succeeds, continue each WAL transition with `--maximum-transitions 1`, stop again at the sealed final manifest for separate `expectedReleaseId` approval, and only deploy Vercel after init/bootstrap/verify-export completes.
